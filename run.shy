#!/bin/bash
echo "compilando cliente..."
g++ src/cliente.cpp -o cliente
if [ $? -eq 0 ]; then
    echo "compilacion correcta"
    echo "ejecutando cliente.."
    ./cliente
else
    echo "error en compilacion"
fi
